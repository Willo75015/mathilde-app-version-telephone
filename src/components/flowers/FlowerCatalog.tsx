import React from 'react'

const FlowerCatalog: React.FC = () => {
  return (
    <div>
      {/* FlowerCatalog component placeholder */}
      <p className="text-gray-500">FlowerCatalog component à implémenter</p>
    </div>
  )
}

export default FlowerCatalog